import styles from '@renderer/assets/css/modals/modal.module.css';

export function ContextualModal(props: { context: JSX.Element }): JSX.Element {
    return <div className={styles.modelContainer}>asxsaxasax{props.context}</div>;
}
